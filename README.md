# Ship-Detection-in-SAR-Imagery-using-YOLO11n
This project focuses on detecting ships in Synthetic Aperture Radar (SAR) imagery using a pretrained YOLO11n model. The model was fine-tuned on the SSDD dataset and evaluated using precision, recall, mAP, PR curves, and qualitative prediction results. The repository contains the training notebook and evaluation outputs.
